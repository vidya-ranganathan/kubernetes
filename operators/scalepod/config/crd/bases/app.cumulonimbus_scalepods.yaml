---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.12.0
  name: scalepods.app.cumulonimbus
spec:
  group: app.cumulonimbus
  names:
    kind: scalepod
    listKind: scalepodList
    plural: scalepods
    singular: scalepod
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.replicas
      name: Desired
      type: string
    - jsonPath: .status.availableReplicas
      name: Available
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: scalepod is the Schema for the scalepods API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: scalepodSpec defines the desired state of scalepod
            properties:
              replicas:
                description: json flag is added for serialisation..
                format: int32
                maximum: 10
                minimum: 1
                type: integer
            type: object
          status:
            description: scalepodStatus defines the current status of scalepod
            properties:
              availableReplicas:
                format: int32
                type: integer
              podNames:
                items:
                  type: string
                type: array
            required:
            - availableReplicas
            - podNames
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
